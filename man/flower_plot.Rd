% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flower_plot.R
\name{flower_plot}
\alias{flower_plot}
\title{Generate Flower Plot for Genome Analysis}
\usage{
flower_plot(
  file_path = NULL,
  data = NULL,
  output_dir = NULL,
  label_outer = TRUE,
  save_plot = TRUE
)
}
\arguments{
\item{file_path}{The file path to the input data (e.g., a \code{.xls} or \code{.csv} file). If \code{NULL}, the function will look for a default file in the current working directory under \code{"Results/stats.xls"}.}

\item{data}{An optional data frame containing the required columns: \code{'Organism name'}, \code{'No. of core genes'}, \code{'No. of unique genes'}, \code{'No. of accessory genes'}, and \code{'No. of exclusively absent genes'}. If provided, this will override the file input.}

\item{output_dir}{The directory where the resulting flower plot PDF will be saved. If \code{NULL}, the plot will be saved in the current working directory.}

\item{label_outer}{Logical. If \code{TRUE}, aligns labels for organisms on the outer side of the petals. Defaults to \code{TRUE}.}

\item{save_plot}{Logical. If \code{TRUE} (default), saves the plot as a PDF. If \code{FALSE}, the plot will not be saved.}
}
\value{
A ggplot object representing the flower plot.
}
\description{
This function generates a flower plot to visualize core, unique, accessory, and absent genes for various organisms.
}
\examples{
# Generate and save the flower plot in the current directory
flower_plot()

# Use custom input data and save to a specific directory
flower_plot(data = custom_data, output_dir = "path/to/output")

# Generate plot with inner-aligned labels without saving
flower_plot(file_path = "path/to/stats.xls", label_outer = FALSE, save_plot = FALSE)
}
