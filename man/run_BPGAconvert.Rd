% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_BPGAconvert.R
\name{run_BPGAconvert}
\alias{run_BPGAconvert}
\title{BPGA Conversion and Table Generation}
\usage{
run_BPGAconvert(
  directory = NULL,
  save_output = FALSE,
  Table_factor = c("locus_tag", "product", "Sequence_id")
)
}
\arguments{
\item{directory}{A character string specifying the working directory where the required files and outputs are located. Defaults to the current working directory.}

\item{save_output}{Logical. If \code{TRUE} (default is \code{FALSE}), intermediate and final outputs will be saved to files.}

\item{Table_factor}{A character vector of column names from \code{PanDB_df} used for generating factor-specific tables. Defaults to \code{c("locus_tag", "product", "Sequence_id")}.}
}
\value{
A combined table (\code{casting_merge}) of comparative genomics data, saved as an Excel file if \code{save_output} is \code{TRUE}.
}
\description{
This function orchestrates the generation of comparative genomics tables using BPGA pipeline outputs.
It processes PanDB data, combines genome summary information, and generates factor-specific tables.
}
\examples{
# Run the function with default settings
run_BPGAconvert()

# Run with a specified directory and save the outputs
run_BPGAconvert(directory = "path/to/working/directory", save_output = TRUE)

# Generate tables for specific factors
run_BPGAconvert(Table_factor = c("locus_tag", "Preferred_name"))
}
