% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_BPGAplot.R
\name{combined_plot}
\alias{combined_plot}
\title{Generate Combined Plot for Flower, Pan-Core, and Phylogeny}
\usage{
combined_plot(
  directory = NULL,
  save_combined = TRUE,
  combined_filename = "combined_plot.pdf"
)
}
\arguments{
\item{directory}{A character string specifying the working directory where the required files and outputs are located. Defaults to the current working directory.}

\item{save_combined}{Logical. If \code{TRUE} (default), the combined plot will be saved as a PDF.}

\item{combined_filename}{A character string specifying the filename for the combined plot PDF. Defaults to \code{"combined_plot.pdf"}.}
}
\value{
A combined plot object.
}
\description{
This function generates a combined plot of the flower plot, pan-core plot, and phylogeny plot.
The plots are labeled "a", "b", and "c" respectively, and are arranged in a grid format.
}
\examples{
# Generate and save the combined plot in the current directory
combined_plot()

# Generate the combined plot without saving
combined_plot(save_combined = FALSE)

# Save the combined plot to a specific directory
combined_plot(directory = "path/to/output", combined_filename = "custom_combined_plot.pdf")
}
